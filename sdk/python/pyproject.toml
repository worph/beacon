[project]
name = "mcp-announce"
version = "0.1.0"
description = "UDP discovery responder SDK for MCP Aggregator"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["mcp_announce"]
